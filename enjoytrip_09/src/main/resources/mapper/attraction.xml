<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.enjoytrip.attraction.model.mapper.AttractionMapper">

	<select id="searchTrip" parameterType="searchConditionDto" resultType="attractionDto">
		select content_type_id contentTypeId, title, addr1 addr, first_image image, latitude, longitude 
		from attraction_info
		where sido_code = #{sido} and gugun_code = #{gugun} and content_type_id = #{contentType}
	</select>
	  
	<select id="searchGugun" parameterType="int" resultType="map">
		select gugun_code code, gugun_name name
		from gugun
		where sido_code = #{sido}
	</select>
	
</mapper>